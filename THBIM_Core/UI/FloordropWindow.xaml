<Window x:Class="THBIM.Tools.UI.FloordropWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="THBIM - Auto Dim Floor Drop"
        Height="380" Width="460"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="#FFF9F7F0"
        FontFamily="Segoe UI">

    <Window.Resources>
        <SolidColorBrush x:Key="Primary" Color="#D9A566"/>
        <SolidColorBrush x:Key="PrimaryDark" Color="#C68C3E"/>
        <SolidColorBrush x:Key="TextGray" Color="#555555"/>
        <SolidColorBrush x:Key="LightBorder" Color="#FFE6D6B8"/>

        <Style x:Key="SectionCard" TargetType="Border">
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="BorderBrush" Value="{StaticResource LightBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#999" BlurRadius="10" ShadowDepth="2" Opacity="0.25"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="HeaderBar" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource Primary}"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="CornerRadius" Value="8,8,0,0"/>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Background" Value="{StaticResource Primary}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryDark}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryDark}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#B57B2E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="RadioButton">
            <Setter Property="Margin" Value="0,0,20,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="{StaticResource TextGray}"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Border Style="{StaticResource SectionCard}">
            <DockPanel>
                <Border Style="{StaticResource HeaderBar}" DockPanel.Dock="Top">
                    <TextBlock Text="Floor Drop Annotation Tool" Foreground="White" FontWeight="Bold" FontSize="15"/>
                </Border>

                <Grid Margin="25,20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Select Annotation Family:" 
                               Foreground="{StaticResource TextGray}" 
                               FontWeight="SemiBold" Margin="0,0,0,8"/>

                    <ComboBox Grid.Row="1" x:Name="cmbFamilies" 
                              Height="40" 
                              Padding="10,0"
                              VerticalContentAlignment="Center"
                              DisplayMemberPath="Name"
                              FontSize="13"/>

                    <TextBlock Grid.Row="1" Margin="0,45,0,0"
                               Text="*Selected family will be used to tag the floor drop elevation."
                               Foreground="#999" FontStyle="Italic" FontSize="11" 
                               VerticalAlignment="Bottom"/>

                    <Grid Grid.Row="2" Margin="0,25,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="Which side is your family drop?" 
                                       Foreground="{StaticResource TextGray}" 
                                       FontWeight="SemiBold" FontSize="12" Margin="0,0,0,8"/>
                            <StackPanel Orientation="Horizontal">
                                <RadioButton x:Name="rbLeft" Content="Left" GroupName="DropSide" IsChecked="True"/>
                                <RadioButton x:Name="rbRight" Content="Right" GroupName="DropSide"/>
                            </StackPanel>
                        </StackPanel>

                        <Button Grid.Column="1" x:Name="btnUpdate" 
                                Content="Update" 
                                Style="{StaticResource PrimaryButton}" 
                                Height="35" Width="100"
                                FontSize="13"
                                VerticalAlignment="Bottom"
                                Click="btnUpdate_Click"/>
                    </Grid>

                    <Button Grid.Row="4" x:Name="btnApply" 
                            Content="START PICKING POINT" 
                            Style="{StaticResource PrimaryButton}" 
                            Height="50" FontSize="15" FontWeight="Bold"
                            Click="BtnApply_Click"
                            Margin="0,20,0,0"/>
                </Grid>
            </DockPanel>
        </Border>
    </Grid>
</Window>