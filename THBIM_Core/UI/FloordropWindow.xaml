<Window x:Class="THBIM.Tools.UI.FloordropWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="THBIM - Floor Drop"
        SizeToContent="Height" Width="460"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="#FFF9F7F0"
        FontFamily="Segoe UI">

    <Window.Resources>
        <SolidColorBrush x:Key="Primary" Color="#D9A566"/>
        <SolidColorBrush x:Key="PrimaryDark" Color="#C68C3E"/>
        <SolidColorBrush x:Key="TextGray" Color="#555555"/>
        <SolidColorBrush x:Key="LightBorder" Color="#FFE6D6B8"/>

        <Style x:Key="SectionCard" TargetType="Border">
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="BorderBrush" Value="{StaticResource LightBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#999" BlurRadius="10" ShadowDepth="2" Opacity="0.25"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="HeaderBar" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource Primary}"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="CornerRadius" Value="8,8,0,0"/>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Background" Value="{StaticResource Primary}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryDark}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryDark}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#B57B2E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Border Style="{StaticResource SectionCard}">
            <DockPanel>
                <Border Style="{StaticResource HeaderBar}" DockPanel.Dock="Top">
                    <TextBlock Text="Floor Drop Annotation Tool" Foreground="White" FontWeight="Bold" FontSize="15"/>
                </Border>

                <StackPanel Margin="20,15,20,20">

                    <!-- Preview Illustration -->
                    <Border Background="#FFFAF5EE" CornerRadius="6" BorderBrush="{StaticResource LightBorder}" BorderThickness="1" Padding="0" Margin="0,0,0,14">
                        <Canvas x:Name="previewCanvas" Height="180" ClipToBounds="True" Loaded="PreviewCanvas_Loaded"/>
                    </Border>

                    <Button x:Name="btnUpdate"
                            Content="UPDATE ALL IN VIEW"
                            Style="{StaticResource PrimaryButton}"
                            Height="40" FontSize="13"
                            Click="btnUpdate_Click"/>

                    <Button x:Name="btnApply"
                            Content="START PICKING POINT"
                            Style="{StaticResource PrimaryButton}"
                            Height="50" FontSize="15" FontWeight="Bold"
                            Click="BtnApply_Click"
                            Margin="0,12,0,0"/>
                </StackPanel>
            </DockPanel>
        </Border>
    </Grid>
</Window>
