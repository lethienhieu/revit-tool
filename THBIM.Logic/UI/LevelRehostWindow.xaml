<Window x:Class="LevelRehost.UI.LevelRehostWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="THBIM - Level Rehost"
        Height="434" Width="700"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="#FFF9F7F0"
        FontFamily="Segoe UI">

    <Window.Resources>
        <SolidColorBrush x:Key="Primary" Color="#D9A566"/>
        <SolidColorBrush x:Key="PrimaryDark" Color="#C68C3E"/>
        <SolidColorBrush x:Key="TextGray" Color="#555555"/>
        <SolidColorBrush x:Key="LightBorder" Color="#FFE6D6B8"/>

        <Style x:Key="SectionCard" TargetType="Border">
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="BorderBrush" Value="{StaticResource LightBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#999" BlurRadius="10" ShadowDepth="2" Opacity="0.25"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="HeaderBar" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource Primary}"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="CornerRadius" Value="8,8,0,0"/>
        </Style>

        <Style x:Key="CleanCheckbox" TargetType="CheckBox">
            <Setter Property="Margin" Value="0,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="{StaticResource TextGray}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Background" Value="{StaticResource Primary}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryDark}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource PrimaryDark}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#DDD"/>
                                <Setter Property="BorderBrush" Value="#CCC"/>
                                <Setter Property="Foreground" Value="#999"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="BorderBrush" Value="#CCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F0F0F0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Style="{StaticResource SectionCard}">
            <DockPanel>
                <Border Style="{StaticResource HeaderBar}" DockPanel.Dock="Top">
                    <TextBlock Text="1. Filter &amp; Selection" Foreground="White" FontWeight="Bold" FontSize="14"/>
                </Border>

                <Grid Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Categories to include:" Foreground="{StaticResource TextGray}" FontWeight="SemiBold" Margin="0,0,0,10"/>


                    <StackPanel Grid.Row="1">
                        <CheckBox x:Name="chkWall" Content="Walls" IsChecked="True" Style="{StaticResource CleanCheckbox}"/>
                        <CheckBox x:Name="chkFloor" Content="Floors" IsChecked="True" Style="{StaticResource CleanCheckbox}"/>
                        <CheckBox x:Name="chkDoor" Content="Doors" IsChecked="True" Style="{StaticResource CleanCheckbox}"/>
                        <CheckBox x:Name="chkColumn" Content="Structural Columns" IsChecked="True" Style="{StaticResource CleanCheckbox}"/>
                        
                        
                    </StackPanel>

                    <Button Grid.Row="2" x:Name="btnSelect" 
                            Content="PICK ELEMENTS" 
                            Style="{StaticResource SecondaryButton}" 
                            Margin="0,20,0,0" Height="40"
                            Click="BtnSelect_Click"/>
                </Grid>
            </DockPanel>
        </Border>

        <Border Grid.Column="1" Style="{StaticResource SectionCard}">
            <DockPanel>
                <Border Style="{StaticResource HeaderBar}" DockPanel.Dock="Top">
                    <TextBlock Text="2. Re-host Settings" Foreground="White" FontWeight="Bold" FontSize="14"/>
                </Border>

                <Grid Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Margin="0,0,0,20">
                        <TextBlock Text="Selected Elements:" Foreground="{StaticResource TextGray}" FontSize="12"/>
                        <TextBlock x:Name="txtStatus" Text="0 items" Foreground="{StaticResource PrimaryDark}" FontWeight="Bold" FontSize="24" Margin="0,5,0,0"/>
                    </StackPanel>

                    <Separator Grid.Row="1" Margin="0,0,0,20" Background="#EEE"/>

                    <StackPanel Grid.Row="2">
                        <TextBlock Text="Destination Level:" Foreground="{StaticResource TextGray}" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <ComboBox x:Name="cmbLevels" Height="35" Padding="5" VerticalContentAlignment="Center"
                                  DisplayMemberPath="Name"/>

                        <TextBlock Text="*Elements will maintain their current 3D position." 
                                   Foreground="#999" FontStyle="Italic" FontSize="11" TextWrapping="Wrap" Margin="0,10,0,0"/>
                    </StackPanel>

                    <Button Grid.Row="3" x:Name="btnRun" 
                            Content="APPLY NEW LEVEL" 
                            Style="{StaticResource PrimaryButton}" 
                            Height="45" FontSize="14"
                            Click="BtnRun_Click"/>
                </Grid>
            </DockPanel>
        </Border>
    </Grid>
</Window>