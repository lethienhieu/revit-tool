<Window x:Class="THBIM.Views.StructureSyncWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:THBIM.Views"
        mc:Ignorable="d"
        Title="THBIM - Structure Sync Manager"
        Height="600" Width="900"
        WindowStyle="None" ResizeMode="NoResize"
        AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <SolidColorBrush x:Key="Primary" Color="#D9A566"/>
        <SolidColorBrush x:Key="PrimaryDark" Color="#C68C3E"/>
        <SolidColorBrush x:Key="BackgroundBrush" Color="#FFF9F7F0"/>
        <SolidColorBrush x:Key="Danger" Color="#E74C3C"/>
        <SolidColorBrush x:Key="Success" Color="#2ECC71"/>
        <SolidColorBrush x:Key="LightBorder" Color="#FFE6D6B8"/>

        <Style x:Key="SectionCard" TargetType="Border">
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="BorderBrush" Value="{StaticResource LightBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#999" BlurRadius="10" ShadowDepth="2" Opacity="0.15"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="HeaderBar" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource Primary}"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="CornerRadius" Value="8,8,0,0"/>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Background" Value="{StaticResource Primary}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryDark}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#DDD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#CCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F2F2F2"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#999"/>
                                <Setter Property="Foreground" Value="Black"/>
                            </Trigger>

                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E6E6E6"/>
                            </Trigger>

                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#EEE"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#FFF9F7F0"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#FFF0E0"/>
                    <Setter Property="Foreground" Value="Black"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource BackgroundBrush}" CornerRadius="10" BorderBrush="{StaticResource Primary}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="{StaticResource Primary}" CornerRadius="9,9,0,0" MouseDown="Header_MouseDown" Height="40">
                <Grid>
                    <TextBlock Text="THBIM - Structure Sync Manager" Foreground="White" FontWeight="Bold" VerticalAlignment="Center" Margin="15,0,0,0"/>
                    <Button Content="✕" Click="BtnClose_Click" HorizontalAlignment="Right" Width="40" Background="Transparent" BorderThickness="0" Foreground="White" FontSize="16"/>
                </Grid>
            </Border>

            <Grid Grid.Row="1" Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3.5*"/>
                    <ColumnDefinition Width="6.5*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource SectionCard}">
                    <DockPanel>
                        <Border Style="{StaticResource HeaderBar}" DockPanel.Dock="Top">
                            <TextBlock Text="1. Scope &amp; Source" Foreground="White" FontWeight="Bold"/>
                        </Border>
                        <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="10">
                            <StackPanel>
                                <TextBlock Text="Target Categories" FontWeight="SemiBold" Foreground="{StaticResource Primary}" Margin="0,0,0,5"/>
                                <ItemsControl ItemsSource="{Binding SelectedCategoryRows}" x:Name="icCategories">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="0,0,0,8">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <ComboBox Grid.Column="0" Height="32" ItemsSource="{Binding DataContext.AllCategoriesList, RelativeSource={RelativeSource AncestorType=Window}}" SelectedItem="{Binding SelectedCategory, Mode=TwoWay}" DisplayMemberPath="Name" VerticalContentAlignment="Center"/>
                                                <Button Grid.Column="1" Content="✕" Width="32" Height="32" Margin="5,0,0,0" Style="{StaticResource SecondaryButton}" Click="BtnRemoveCat_Click"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                <Button Content="+ Add Category" Style="{StaticResource SecondaryButton}" HorizontalAlignment="Left" Margin="0,5,0,15" Click="BtnAddCat_Click"/>
                                <Separator Background="#EEE" Margin="0,0,0,15"/>
                                <CheckBox x:Name="ChkUseLinks" Content="Use Revit Links" FontWeight="SemiBold" Checked="ChkUseLinks_Changed" Unchecked="ChkUseLinks_Changed" Margin="0,0,0,10"/>
                                <Border Background="#FAFAFA" BorderBrush="#EEE" BorderThickness="1" CornerRadius="4" Visibility="{Binding IsChecked, ElementName=ChkUseLinks, Converter={StaticResource BoolToVis}}">
                                    <ListBox x:Name="LbLinks" MaxHeight="200" BorderThickness="0" Background="Transparent" Padding="5">
                                        <ListBox.ItemTemplate>
                                            <DataTemplate>
                                                <CheckBox IsChecked="{Binding IsSelected}" Content="{Binding Name}" Margin="2"/>
                                            </DataTemplate>
                                        </ListBox.ItemTemplate>
                                    </ListBox>
                                </Border>
                            </StackPanel>
                        </ScrollViewer>
                    </DockPanel>
                </Border>

                <Border Grid.Column="1" Style="{StaticResource SectionCard}">
                    <DockPanel>
                        <Border Style="{StaticResource HeaderBar}" DockPanel.Dock="Top">
                            <TextBlock Text="2. Relationships &amp; Sync" Foreground="White" FontWeight="Bold"/>
                        </Border>
                        <Grid Margin="15">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="0">
                                <TextBlock Text="Define New Relationship" FontWeight="SemiBold" Foreground="{StaticResource Primary}" Margin="0,0,0,10"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="40"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Button x:Name="BtnChooseParent" Grid.Column="0" Content="A. Choose Parent" Height="40" Style="{StaticResource PrimaryButton}" Click="BtnChooseParent_Click"/>
                                    <TextBlock Grid.Column="1" Text="➜" FontSize="20" Foreground="#CCC" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    <Button x:Name="BtnChooseChild" Grid.Column="2" Content="B. Choose Child" Height="40" Style="{StaticResource PrimaryButton}" IsEnabled="False" Opacity="0.5" Click="BtnChooseChild_Click"/>
                                </Grid>
                            </StackPanel>

                            <Grid Grid.Row="1" Margin="0,20">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <Grid Margin="0,0,0,5">
                                    <TextBlock Text="Active Pairs List (Click Name to Rename)" FontWeight="SemiBold" Foreground="#555" VerticalAlignment="Center"/>
                                    <Button Content="Clear All" HorizontalAlignment="Right" Click="BtnClear_Click"
                                            Background="Transparent" Foreground="{StaticResource Danger}"
                                            BorderThickness="0" FontWeight="Bold" Cursor="Hand"/>
                                </Grid>

                                <Border Grid.Row="1" BorderBrush="#EEE" BorderThickness="1" CornerRadius="4">
                                    <DataGrid x:Name="DgRelationships" AutoGenerateColumns="False" CanUserAddRows="False">
                                        <DataGrid.Columns>
                                            <DataGridCheckBoxColumn Binding="{Binding IsChecked, UpdateSourceTrigger=PropertyChanged}" Width="40" ElementStyle="{x:Null}"/>

                                            <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" IsReadOnly="False">
                                                <DataGridTextColumn.ElementStyle>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                                        <Setter Property="Padding" Value="5,0"/>
                                                    </Style>
                                                </DataGridTextColumn.ElementStyle>
                                                <DataGridTextColumn.EditingElementStyle>
                                                    <Style TargetType="TextBox">
                                                        <Setter Property="Padding" Value="2"/>
                                                    </Style>
                                                </DataGridTextColumn.EditingElementStyle>
                                            </DataGridTextColumn>

                                            <DataGridTextColumn Header="Qty" Binding="{Binding ChildCount}" Width="60" IsReadOnly="True"/>
                                            <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="80" IsReadOnly="True"/>

                                            <DataGridTemplateColumn Width="40">
                                                <DataGridTemplateColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <Button Content="✕" Width="24" Height="24" Background="{StaticResource Danger}" Foreground="White" BorderThickness="0" Click="BtnDeleteRel_Click"/>
                                                    </DataTemplate>
                                                </DataGridTemplateColumn.CellTemplate>
                                            </DataGridTemplateColumn>
                                        </DataGrid.Columns>
                                    </DataGrid>
                                </Border>
                            </Grid>

                            <Grid Grid.Row="2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="15"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Button Grid.Column="0" Height="45" Content="Check Tracking" Click="BtnTracking_Click">
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
                                            <Setter Property="Background" Value="#E67E22"/>
                                        </Style>
                                    </Button.Style>
                                </Button>
                                <Button Grid.Column="2" Height="45" Content="Sync Position" Click="BtnUpdate_Click">
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
                                            <Setter Property="Background" Value="{StaticResource Success}"/>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </Grid>
                        </Grid>
                    </DockPanel>
                </Border>
            </Grid>

            <Grid Grid.Row="2" Margin="15,0,15,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="TxtStatus" Text="Ready" Foreground="#999" VerticalAlignment="Center" FontStyle="Italic"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="Reset Color" Width="100" Margin="0,0,10,0" Click="BtnResetColor_Click">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource SecondaryButton}">
                                <Setter Property="Foreground" Value="#555"/>
                                <Setter Property="BorderBrush" Value="#999"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#F0F0F0"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                    <Button Content="Close" Width="100" Margin="0,0,10,0" Style="{StaticResource SecondaryButton}" IsCancel="True" Click="BtnClose_Click"/>
                    <Button Content="ACCEPT &amp; FINISH" Width="160" Height="36" Click="BtnAccept_Click" FontWeight="Bold" Style="{StaticResource PrimaryButton}"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
