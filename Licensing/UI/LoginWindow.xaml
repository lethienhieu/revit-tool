<Window x:Class="THBIM.Tools.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="THBIM Login" Height="360" Width="520"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        FontFamily="Segoe UI" Background="#FFF9F7F0" KeyDown="Root_KeyDown">

    <Window.Resources>
        <SolidColorBrush x:Key="Primary"     Color="#D9A566"/>
        <SolidColorBrush x:Key="PrimaryDark" Color="#C68C3E"/>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="MinWidth" Value="96"/>
            <Setter Property="Background" Value="{StaticResource Primary}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryDark}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style x:Key="GhostButton" TargetType="Button">
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="MinWidth" Value="96"/>
            <Setter Property="Background" Value="#FFF5EFE1"/>
            <Setter Property="Foreground" Value="#7A5A2A"/>
            <Setter Property="BorderBrush" Value="#E7D1AC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style x:Key="UnderlinedTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0,0,0,1"/>
                            <Border VerticalAlignment="Bottom"
                                    BorderBrush="#CFCFCF"
                                    BorderThickness="0,0,0,1"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Underlined PasswordBox (FIX) -->
        <Style x:Key="UnderlinedPasswordBox" TargetType="PasswordBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Grid>
                            <!-- PART_ContentHost is required for input -->
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="0,0,0,1"
                                          Background="{TemplateBinding Background}"/>
                            <Border VerticalAlignment="Bottom"
                                    BorderBrush="#CFCFCF"
                                    BorderThickness="0,0,0,1"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SectionHeader" TargetType="Border">
            <Setter Property="Background" Value="#FFF3E6CF"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Style="{StaticResource SectionHeader}">
            <TextBlock Text="THBIM Licensing" FontWeight="Bold" FontSize="18"/>
        </Border>

        <!-- Form -->
        <Grid Grid.Row="1" Margin="0,8,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="18"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="18"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel>
                <TextBlock Text="Email" Margin="0,0,0,4"/>
                <TextBox x:Name="EmailBox" Style="{StaticResource UnderlinedTextBox}"/>
            </StackPanel>

            <StackPanel Grid.Row="2">
                <TextBlock Text="Password" Margin="0,0,0,4"/>
                <PasswordBox x:Name="PwdBox" Style="{StaticResource UnderlinedPasswordBox}"/>
            </StackPanel>

            <TextBlock Grid.Row="4"
           Foreground="#777"
           FontSize="12">
    <Run Text="Don't have an account? Click "/>
    <Hyperlink NavigateUri="https://thbim.pages.dev"
               RequestNavigate="Hyperlink_RequestNavigate">
        <Run Text="thbim.pages.dev"
             TextDecorations="Underline"/>
    </Hyperlink>
            </TextBlock>
            
        </Grid>

        <!-- Buttons -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,12,0,0">
            <Button x:Name="SignupBtn"
                    Content="Sign Up"
                    Style="{StaticResource GhostButton}"
                    Click="Signup_Click"/>
            <Button x:Name="LoginBtn"
                    Content="Log In"
                    Margin="8,0,0,0"
                    Style="{StaticResource PrimaryButton}"
                    Click="Login_Click"/>
        </StackPanel>

        <!-- Busy overlay -->
        <Grid x:Name="BusyOverlay" Visibility="Collapsed" Background="#66000000">
            <Border Background="White"
                    CornerRadius="8"
                    Padding="16"
                    Width="320"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    BorderBrush="#DDD"
                    BorderThickness="1">
                <StackPanel>
                    <TextBlock x:Name="BusyTitle"
                               Text="Processing…"
                               FontWeight="SemiBold"
                               Margin="0,0,0,10"
                               HorizontalAlignment="Center"/>
                    <Grid Height="6" MinWidth="240">
                        <Border Background="#EEE" CornerRadius="3"/>
                        <Border Background="{StaticResource Primary}" CornerRadius="3">
                            <Border.RenderTransform>
                                <ScaleTransform x:Name="BusyScale"
                                                ScaleX="0"
                                                ScaleY="0.5"/>
                            </Border.RenderTransform>
                            <Border.RenderTransformOrigin>0,0.5</Border.RenderTransformOrigin>
                        </Border>
                    </Grid>
                    <TextBlock x:Name="BusyNote"
                               Text="Please wait a moment"
                               Foreground="#777"
                               Margin="0,10,0,0"
                               FontSize="12"
                               HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
