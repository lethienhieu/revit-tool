<Window x:Class="THBIM.Tools.FeedbackWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="THBIM - Feedback / Report" Height="500" Width="560"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">

    <Window.Resources>
        <!-- Palette -->
        <SolidColorBrush x:Key="Primary"      Color="#D9A566"/>
        <SolidColorBrush x:Key="PrimaryDark"  Color="#C68C3E"/>

        <!-- Underlined TextBox with placeholder (bind to Tag) -->
        <Style x:Key="UnderlinedTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Height" Value="26"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0,0,0,1"/>
                            <Border VerticalAlignment="Bottom"
                                    BorderBrush="#CFCFCF"
                                    BorderThickness="0,0,0,1"/>
                            <TextBlock Text="{TemplateBinding Tag}"
                                       Foreground="#999"
                                       Margin="2,0,0,0"
                                       IsHitTestVisible="False"
                                       VerticalAlignment="Center">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource TemplatedParent}}" Value="">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Underlined ComboBox (dropdown) with placeholder via Tag -->
        <Style x:Key="UnderlinedComboBox" TargetType="ComboBox">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Background" Value="Transparent"/>

            <!-- Display each item using ToolChoice.Display -->
            <Setter Property="ItemTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock Text="{Binding Display}" />
                    </DataTemplate>
                </Setter.Value>
            </Setter>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                          Grid.ZIndex="2"
                                          Focusable="False"
                                          Background="Transparent"
                                          BorderThickness="0"
                                          IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"/>

                            <!-- Selected item display -->
                            <ContentPresenter Margin="0,0,24,0"
                                              HorizontalAlignment="Left"
                                              VerticalAlignment="Center"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"/>

                            <!-- Placeholder when nothing is selected -->
                            <TextBlock Margin="2,0,24,0"
                                       Foreground="#999"
                                       VerticalAlignment="Center">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}"/>
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding SelectedIndex, RelativeSource={RelativeSource TemplatedParent}}" Value="-1">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding SelectedItem, RelativeSource={RelativeSource TemplatedParent}}" Value="{x:Null}">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>

                            <Path HorizontalAlignment="Right"
                                  VerticalAlignment="Center"
                                  Margin="0,0,4,0"
                                  Data="M 0 0 L 4 4 L 8 0"
                                  Stroke="#777"
                                  StrokeThickness="1.6"
                                  StrokeEndLineCap="Round"
                                  StrokeStartLineCap="Round"/>

                            <Border VerticalAlignment="Bottom"
                                    BorderBrush="#CFCFCF"
                                    BorderThickness="0,0,0,1"/>

                            <Popup x:Name="PART_Popup"
                                   AllowsTransparency="True"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   Focusable="False">
                                <Border Background="White"
                                        BorderBrush="#DDD"
                                        BorderThickness="1"
                                        Padding="2"
                                        SnapsToDevicePixels="True">
                                    <ScrollViewer CanContentScroll="True">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger SourceName="ToggleButton" Property="IsChecked" Value="True">
                                <Setter TargetName="PART_Popup" Property="IsOpen" Value="True"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Buttons -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="MinWidth" Value="110"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Background" Value="{StaticResource Primary}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryDark}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style x:Key="GhostButton" TargetType="Button">
            <Setter Property="MinWidth" Value="96"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Background" Value="#FFF5EFE1"/>
            <Setter Property="Foreground" Value="#7A5A2A"/>
            <Setter Property="BorderBrush" Value="#E7D1AC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <!-- Text styles -->
        <Style x:Key="HeaderText" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Foreground" Value="#5A3F1C"/>
        </Style>

        <Style x:Key="LabelText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#444"/>
            <Setter Property="Margin" Value="0,12,0,0"/>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header strip -->
        <Border Grid.Row="0"
                Background="#FFF3E6CF"
                CornerRadius="8"
                Padding="12"
                Margin="0,0,0,10">
            <TextBlock Text="Feedback / Report" Style="{StaticResource HeaderText}"/>
        </Border>

        <!-- Email + Subject -->
        <Grid Grid.Row="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="Email" Style="{StaticResource LabelText}"/>
                <TextBox x:Name="EmailBox"
                         Style="{StaticResource UnderlinedTextBox}"
                         Tag="you@gmail.com"
                         IsReadOnly="True"
                         IsTabStop="False"
                         Focusable="False"/>
            </StackPanel>

            <StackPanel Grid.Column="2">
                <TextBlock Text="Subject" Style="{StaticResource LabelText}"/>
                <TextBox x:Name="SubjectBox"
                         Style="{StaticResource UnderlinedTextBox}"
                         Tag="User feedback"
                         IsReadOnly="True"
                         IsTabStop="False"
                         Focusable="False"/>
            </StackPanel>
        </Grid>

        <!-- Tool selector -->
        <StackPanel Grid.Row="2" Margin="0,0,0,8">
            <TextBlock Text="Select tool" Style="{StaticResource LabelText}"/>
            <!-- Always show placeholder 'ALL - TH SUITE' when not selected -->
            <ComboBox x:Name="ToolCombo"
                      Style="{StaticResource UnderlinedComboBox}"
                      Tag="ALL - TH SUITE"/>
        </StackPanel>

        <!-- Body -->
        <StackPanel Grid.Row="3">
            <TextBlock Text="Message" Style="{StaticResource LabelText}"/>
            <TextBox x:Name="BodyBox"
                     Margin="0,4,0,0"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     Height="170"
                     BorderThickness="1"
                     BorderBrush="#E9E0A7"
                     Background="White"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,12,0,0">
            <Button Content="Cancel"
                    Width="90"
                    Margin="0,0,8,0"
                    Style="{StaticResource GhostButton}"
                    Click="Cancel_Click"/>
            <Button x:Name="SendBtn"
                    Content="Send"
                    Width="110"
                    Style="{StaticResource PrimaryButton}"
                    Click="Send_Click"/>
        </StackPanel>

        <!-- === Busy Overlay (dim background + smooth gold progress bar) === -->
        <Grid x:Name="BusyOverlay" Visibility="Collapsed" Background="#66000000">
            <Border Background="White"
                    CornerRadius="8"
                    Padding="16"
                    Width="300"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    BorderBrush="#DDD"
                    BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="Sending feedback…"
                               FontWeight="SemiBold"
                               Margin="0,0,0,10"
                               HorizontalAlignment="Center"/>

                    <!-- Track -->
                    <Grid x:Name="BusyTrack" Height="6" MinWidth="220">
                        <Border Background="#EEE" CornerRadius="3"/>
                        <!-- Animated gold bar using ScaleTransform -->
                        <Border Background="#D9A566" CornerRadius="3">
                            <Border.RenderTransform>
                                <ScaleTransform x:Name="BusyScale" ScaleX="0" ScaleY="0.5"/>
                            </Border.RenderTransform>
                            <Border.RenderTransformOrigin>0,0.5</Border.RenderTransformOrigin>
                        </Border>
                    </Grid>

                    <TextBlock x:Name="BusyNote"
                               Text="Please wait a moment"
                               Foreground="#777"
                               Margin="0,10,0,0"
                               FontSize="12"
                               HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>

    </Grid>
</Window>
